[build-system]
requires = ["setuptools>=70"]
build-backend = "setuptools.build_meta"

[project]
name = "WalkmanPlaylistManager"
version = "1.0.0"
requires-python = ">=3.13"
readme = "README.md"

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "src"]
